# HTL-Leonding KeyCloak Auth Library - Server

- Provides additional functionality to the `LeoAuth` library for server-side applications.
- Including this package will also include the `LeoAuth` package.